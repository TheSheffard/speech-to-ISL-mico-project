const Footer = () => {
  return (
    <footer className="mx-auto max-w-6xl">
      <div className="flex flex-col items-center justify-between gap-2 border-t border-line pt-4 text-[12px] text-muted sm:flex-row">
        <span>SoundSigns · Voice to Indian Sign Language</span>
        <span>First translation may take a moment while the server wakes up.</span>
      </div>
    </footer>
  );
};

export default Footer;
